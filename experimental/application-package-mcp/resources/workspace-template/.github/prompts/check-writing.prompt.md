# Check Selected Writing

Send only the selected text to the MCP checker.

Choose mode:

- `application`
- `academic`
- `blog`
- `work`
- `social`
- `general`

Save the checker result under `outputs/<target>/` or summarize it in `memory/benchmark-results.md`.

