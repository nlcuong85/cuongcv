# Student Writing Instructions

Use `profile/` and `memory/` as source of truth.

For old workspaces, run `scripts/migrate_legacy_workspace.py .` first.

Do not delete old `candidate/`, `jobs/`, or `outputs/` folders.

