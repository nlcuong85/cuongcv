# Copilot Context Pack

This file can be regenerated with:

```bash
python3 scripts/build_copilot_pack.py . --out copilot_context.md
```

Until it is regenerated, use `copilot_quick_context.md` and `AGENTS.md`.

